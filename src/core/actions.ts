
// 基础服务方法
export enum BaseActions {
  // http 请求
  REQUEST = 'request',
}

// 聚合服务方法
export enum PolymerizeActions {
  // zip 压缩
  ZIP = 'zip',
}

// SDK服务方法
export enum SDKActions {
  // OCR
  OCR = 'orc',
}
